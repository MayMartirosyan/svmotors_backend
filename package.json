{
  "name": "hraviratoms-super-admin",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node dist/app.js",
    "build-frontend": "cd frontend && npm install && npm run build",
    "build-backend": "cd backend && npm install && npm run build",
    "build-and-start": "npm run build-frontend && npm run build-backend && npm run start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "deploy": "eb deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
